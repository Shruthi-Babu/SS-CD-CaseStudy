#include <stdio.h>	//to include header files
int main()	//start point of the program
{
   // printf() displays the string inside quotation
   printf("Hello, World!");
   int a=2, b=3;	/* initialise two numbers
with two values */
   return a+b;	//final answer
}
